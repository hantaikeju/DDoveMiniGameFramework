---
type: Playbook
title: 开工评分
description: 开工班组冻结的评分尺子。先分后改尺。
tags: [索引, agent]
status: draft
generated: { by: human:cjh, at: 2026-09-17T08:40:00Z }
sources:
  - id: crew
    resource: /00-索引/Agent/开工班组.md
    title: 开工班组
  - id: score
    resource: ../../../.agents/skills/ddove-work/roles/score.md
    title: 评分角色卡
---

# 开工评分

Concept ID：`/00-索引/Agent/开工评分`。地图：[开工班组](/00-索引/Agent/开工班组.md)。

规格版本: v10

过线：总分 ≥ 70（百分）。

| 维 | 权重 | 看什么 |
|----|------|--------|
| 需求验收 | 20 | 审查 PASS 即本维满分。同票 NEED_PATCH 经父会话裁决后重派 1 次且最终 PASS，不扣。稿与实现冲突以代码为准，且须回写入选稿 |
| Wiki 同步 | 20 | 只 update/create 入选需求稿与本规格篇。wiki 技能改 index/_log 不算越界。禁止新建工程概念篇、禁止 update 对照篇。打分前不改规格篇不扣；规格篇由父会话按 SCORE 的 vN+1 再 update |
| 职责边界 | 20 | 未扫 `Library`、未顺手建工程篇、未升 `stable`、配表走 config |
| 范围 | 15 | 工人只动票上 `paths`。票上已列文件的同路径 `.meta` 视为票内，不必自报。例外（须同时满足才不扣）：① 改的是同批其它票已产出文件的 `.meta`（含所在目录 `.meta`）；② 仅为 Prefab/组件 GUID 绑定；③ STATUS 越界栏自报了路径。未自报、或改了 `.cs` / `.prefab` / `.unity` 等本体，仍按越界扣（每多一类非 `.meta` 路径扣 5，下限 0）。范围按票独立：他票或前刀已在工作区的文件不株连本票 |
| 信任表述 | 15 | 结论须点名入选稿 `draft` 与本规格篇 `draft`/`stable`。对照篇写「过期」即可，不得把对照篇改 `deprecated` 或动其 status。入选稿回写后仍 draft、仍带 `需求` 不扣（摘 tag 在过线且审查过关之后） |
| 规格增量 | 10 | 本轮是否交出可执行的 vN+1 |

## 变更史

- v10：消化 LoopList 进退场五票（tween-iface / scroll-item-tween / loop-motion / sample-v-anim / sample-h-anim）首轮干净过线。v9 条文够用。审查 PASS 且验收 1–14 全部满足时，需求验收仍满分，不按条数拆分。五票越界栏均为无；票上 `.cs` / `.unity` / `.prefab` 及同路径 `.meta` 属票内。回写可把 description 写成落地行为，只要仍 draft、仍带需求、不写 verified。对照篇只在入选稿点名过期且不 update、不改 status，不扣。当日 log 里先有的 grill（stable→draft）属前序；index_cjh 落稿时改 LoopList 一行徽章属 wiki 技能，不扣。打分前规格仍停在 v9 不扣。
- v9：消化 LoopList 格子 tween 四票（tween-iface / scroll-item-tween / demorow-unscale / sample-attach）首轮干净过线。v8 条文够用。审查 PASS（本刀验收 14，1–13 沿用）时，需求验收仍满分，不按条数拆分。`scroll-item-tween` 票上的 `ScrollItemTween.cs` 与同路径 `.meta` 属票内，不必另报越界；其余三票所列 `.cs` / `.prefab` / `.unity` 的同路径 `.meta` 同样属票内。回写可把 description 写成落地行为，改正文方法名（`Apply` / `ResetScale`），并在 sources 补 `ScrollItemTween.cs`，只要 frontmatter 仍 `draft`、tags 仍含 `需求`、不写 `verified`。对照篇只在入选稿点名过期（PrimeTween、UI业务封装、DDoveUI、WndHomeSample入口）且不 update、不改其 status，Wiki 同步不扣。当日 log 里先有的本刀 grill（stable → draft），以及更早的升 stable / 摘 tag / 开工评分 v7→v8，属前序。打分前规格仍停在 v8 不扣。index 仍停在上一刀摘要、本轮未改 index，不扣。
- v8：消化 LoopList 拆 partial 一票（`loop-list-partial-split`）首轮干净过线。v7 条文够用。票上列出的新 partial（`LoopList.Layout.cs` / `LoopList.Pool.cs` / `LoopList.Preview.cs`）及这三份同路径 `.meta` 属票内，不必另报越界。只搬代码、审查 PASS（本刀验收 13，其余沿用）时，需求验收仍满分，不按条数拆分。回写可把 description 写成落地行为，并在 sources 补新 partial 路径，只要 frontmatter 仍 `draft`、tags 仍含 `需求`、不写 `verified`。对照篇只在入选稿点名过期且不 update，Wiki 同步不扣。当日 log 里先有的本刀 grill（stable → draft），以及更早的升 stable / 摘 tag / 开工评分 v6→v7，属前序。打分前规格仍停在 v7 不扣。
- v7：消化 LoopList 返回与中心缩放两票（loop-keep-source / demo-center-scale）首轮干净过线。v6 条文够用。本刀验收 9、11、12 与沿用的 1–8、10 一并 PASS 时，需求验收仍按「审查 PASS 即满分」，不按条数拆分。description 写成落地行为（Hide 留 IList、Show 保滚动位置、DemoRow 正中 1 / 两端 0.85、Home 不缩放）、「要什么」仍是规格正文，只要 frontmatter 仍 `draft`、tags 仍含 `需求`、不写 `verified`，信任表述不扣。对照篇只在入选稿点名过期且不 update，Wiki 同步不扣。当日 log 里先有的本刀 grill 打回 draft，以及更早条目里的升 stable / 摘 tag，属前序，不当成本刀越界。
- v6：消化 LoopList 横向三票（loop-axis / loop-h-window / home-looph）首轮干净过线。v5 条文够用。同一 Concept 再次入选（grill 从 stable 回到 draft 且重新带 `需求`）按新刀打；前刀已在工作区的文件不株连本刀票。回写可以把 description / 「要什么」写成「已按稿落地」，只要 frontmatter 仍 `status: draft`、tags 仍含 `需求`、不写 `verified`，信任表述不扣。当日 log 里先有的 grill 打回 draft 条目，不当成升 stable 或摘 tag。index 仍停在上一刀摘要、本轮未改 index，Wiki 同步不扣。
- v5：消化 WndHome Sample 四票（launch-nav / collect-demo / loop-back / home-entry）首轮干净过线。v4 条文够用。入选稿可同时点名多篇对照「过期」；只要不 update 那些篇、不改其 status，Wiki 同步与信任表述都不扣。
- v4：消化 LoopList 第二刀干净过线。打分前不改规格篇不扣；回写后仍 draft+需求不扣；范围按票独立、前刀文件不株连。
- v3：消化 loop-demo Prefab GUID `.meta` 自报越界；同批票脚本 `.meta` 仅绑 GUID 且自报则不扣。
- v2：消化 t5 编辑器占用重派；明确对照篇只点名过期、重派 1 次过线不扣需求验收。
- v1：种子。过线 70。
