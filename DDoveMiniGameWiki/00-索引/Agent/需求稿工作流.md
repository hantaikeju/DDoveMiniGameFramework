---
type: Playbook
title: 需求稿工作流
description: grill 对齐后落 draft 需求稿；整篇处理完再 change-state 入正式库。不另造 status。
tags: [索引, agent]
status: stable
generated: { by: human:cjh, at: 2026-09-09T08:11:00Z }
verified: { by: human:cjh, at: 2026-09-17T07:33:00Z }
sources:
  - id: okf
    resource: /00-索引/OKF约定.md
    title: OKF 约定与检索
  - id: map
    resource: /00-索引/Agent/Agent总览.md
    title: Agent 总览
  - id: grill
    resource: ../../../.agents/skills/ddove-grill/SKILL.md
    title: ddove-grill
  - id: opening
    resource: /00-索引/Agent/开篇与清单徽章.md
    title: 开篇与清单徽章
---

# 需求稿工作流

Concept ID：`/00-索引/Agent/需求稿工作流`。地图：[Agent 总览](/00-索引/Agent/Agent总览.md)。

`status` 只允许 `draft` / `stable` / `deprecated`。需求「处理中」= `draft` + `tags` 含 `需求`。入正式库 = `stable`（change-state 同步写 `verified`，并摘掉 `需求`）。正文不自报 `status`，细则：[开篇与清单徽章](/00-索引/Agent/开篇与清单徽章.md)。

工程需求落 `01`–`04`（职种 `tags` + `需求`）。AI 用法需求落本目录（`tags: [索引, agent, 需求]`）。

## 步骤

1. `/ddove-grill`：拷问到 frontier 空、你确认对齐。
2. 落稿：grill 末步或 `/ddove-wiki-create`。`type: Playbook`；`status: draft`；`tags` 按上一节。开篇不写「本篇是 draft」。
3. 按稿改代码或补正文。问答 / 改代码走 `ddove-work`（先检索），结论须标明 draft。已有其它篇过期走 `/ddove-wiki-update`。
4. 整篇能当约定：`/ddove-wiki-change-state` → `stable`（同步写 `verified`，摘 `需求`，只改清单那一行徽章）。
