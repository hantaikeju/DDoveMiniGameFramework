# Directory Update Log

## 2026-09-22
* **Update**: `/02-程序-前/LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v8 → v9。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。按代码回写格子 tween 已落地（`Apply` / `ResetScale`、`ScrollItemTween`、竖横样板挂上）。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。grill 落格子 tween：列表把离视口中心的位置交给 `IScrollItemTween`；`ScrollItemTween` 写中心缩放；没有接口则只滑动。`status` stable → draft，摘 `verified`，加 `需求`。
* **Update**: `/02-程序-前/LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v7 → v8。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。按代码回写四份 partial 已落地（主文件、Layout、Pool、Preview）。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。grill 落拆文件：同一个 partial class 分成入口、Layout、Pool、Preview；只搬代码。`status` stable → draft，摘 `verified`，加 `需求`。
* **Update**: `/02-程序-前/LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v6 → v7。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。按代码回写返回与缩放已落地（OnDisable 不 DropSource，OnEnable 按原位置重刷；DemoRow 正中 1、两端 0.85）。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。grill 落返回与缩放：`Hide` 不 `DropSource`，`Show` 用原数据刷回并保持滚动位置；`DemoRow` 正中 1、视口两端 0.85；Home 不缩放。`status` stable → draft，摘 `verified`，加 `需求`。
* **Update**: `/02-程序-前/LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v5 → v6。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。按代码回写横向已落地（`LoopDirection`、`WndLoopHDemo`、Home 行 `LoopH`）。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。grill 落横向：同一挂件加方向；横样板 `WndLoopHDemo`；Home 多一行 `LoopH`；`ScrollTo` 横用 Left/Middle/Right。`status` stable → draft，摘 `verified`，加 `需求`。
