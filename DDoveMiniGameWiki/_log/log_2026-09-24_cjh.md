# Directory Update Log

## 2026-09-24
* **Update**: `/05-需求/WndHomeSample入口` `status` draft → deprecated。得分留在需求稿。用法篇写到 `/02-程序-前/WndHomeSample入口`。
* **Update**: `/02-程序-前/WndHomeSample入口` 由 Stub 改为用法篇，`status` → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/05-需求/WndHomeSample入口` 去掉已失效的过期对照。启动链与音频根以用法篇为准。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/Play到WndHome`、`/02-程序-前/DDoveUI`、`/02-程序-前/UI业务封装`、`/02-程序-前/DDoveCfg`、`/02-程序-前/DDoveAtlas`、`/02-程序-前/IOC容器使用规范` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/Play到WndHome`、`/02-程序-前/DDoveUI`、`/02-程序-前/UI业务封装`、`/02-程序-前/DDoveCfg`、`/02-程序-前/DDoveAtlas`：Launch 改为装表、`LoadFile`、进 Architecture、图集与音频初始化，再 `NavigateToAsync<WndHome>`。Play 与 DDoveUI 写上首页三行。`status` → draft，摘了 `verified`。
* **Update**: `/02-程序-前/IOC容器使用规范`：音频根改为已落地，链到 DDoveAudio。`status` → draft，摘了 `verified`。
