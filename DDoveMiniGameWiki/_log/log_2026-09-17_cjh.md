# Directory Update Log

## 2026-09-17
* **Update**: `/00-索引/OKF约定` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/Agent/需求稿工作流` `status` draft → stable，已写 `verified`（human:cjh），摘了 `需求`。
* **Update**: `/00-索引/Agent/开篇与清单徽章` 刷新 `verified`（human:cjh），摘了 `需求`；清单该行只留 `Playbook`。
* **Update**: `/00-索引/OKF约定`：change-state 可改本篇清单那一行，仍禁止日常重生成。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/Agent/需求稿工作流`：开篇不自报 status；升正式摘 `需求`。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/Agent/开篇与清单徽章` `status` draft → stable，已写 `verified`（human:cjh）。
* **Add**: `/00-索引/Agent/开篇与清单徽章`。grill 落稿：正文不自报 status；升 stable 摘 `需求`；只改清单那一行徽章。三态绑定不动。`status: draft` + `需求`。
* **Update**: `/02-程序-前/正确路径与程序集方向` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/正确路径与程序集方向`：去掉开篇「本篇是 draft」；「本刀只落稿」改为机械闸尚未落地。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/正确路径与程序集方向` `status` draft → stable，已写 `verified`（human:cjh）。
* **Add**: `/02-程序-前/正确路径与程序集方向`。grill 落稿：五条都写；唯一新闸是框架程序集不得引用或编译到 Game。`status: draft` + `需求`。
* **Update**: `/00-索引/Agent/开工班组`：删开篇「本篇是 draft」。正文已与 `status: stable` 对齐，刷新 `verified`（human:cjh）。
* **Update**: `/00-索引/Agent/开工班组` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/Agent/开工班组`：去掉「ddove-plan 尚未落地」；落点表改为已落地。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/Agent/开工班组` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/Agent/开工班组`：只读 `ddove-plan` 拆图；STATUS 交还；父会话不收 diff 全文。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/Agent/开工班组` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/Agent/开工班组`：对齐已落地子代理（父会话计划/调度；`ddove-worker` 可并行；审查/评分只读）。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/Agent/开工班组` `status` draft → stable，已写 `verified`（human:cjh）。
* **Add**: `/00-索引/Agent/开工班组`。grill 落稿：`ddove-work` 按 `draft`+`需求` 开工；计划同轮写 wiki；审查过关、评分出表并升级 `开工评分`。`status: draft` + `需求`。
