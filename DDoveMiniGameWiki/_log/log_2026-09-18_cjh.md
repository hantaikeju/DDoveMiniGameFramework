# Directory Update Log

## 2026-09-18
* **Update**: `/00-索引/Agent/开工评分`：规格版本 v2 → v3。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList`：按代码回写已落地（`LoopList` / `WndLoopDemo` / 导出剥 DontSave）。点名对照 DDoveUI、UI业务封装过期。`status` 仍 draft，未写 `verified`。
* **Add**: `/02-程序-前/LoopList`。grill 落稿：Game/Mono 定高垂直回收；制作场景预览不进 Prefab；样板 `WndLoopDemo`；`SetCount` + `BindItem`。`status: draft` + `需求`。
