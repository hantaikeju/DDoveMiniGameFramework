# Directory Update Log

## 2026-09-03
* **Update**: 运行时前缀 EU → DDove。`EUFramework`/`EUDebug`/`EURes` 改为 `DDoveFramework`/`DDoveDebug`/`DDoveRes`；wiki 篇改名 `/02-程序-前/DDoveFramework-Core`、`DDoveDebug`、`DDoveRes`。
* **Update**: `/02-程序-前/EURes` `status` draft → stable，写 `verified.by: human:cjh`。
* **Add**: `/02-程序-前/EURes`。YooAsset 门面：建包 / 初始化 / `LoadAssetAsync`；失败 `EUDebug.LogError`，不进 IOC。
* **Update**: `/02-程序-前/EUDebug` `status` draft → stable，写 `verified.by: human:cjh`。
* **Update**: `/02-程序-前/EUDebug`：`Log` / `LogWarning` / `LogError` 保留单字符串重载，原样输出。
* **Add**: `/02-程序-前/EUDebug`。Core 统一日志，直接 `UnityEngine.Debug`；`[Title] (key, value)`；`Log` 挂 `[Conditional("DEBUG")]`。
* **Update**: `/02-程序-前/EUFramework-Core`、`IOC容器`、`Architecture与角色`、`CoreFsm`、`UniTask异步`：`noEngineReferences: false`（只为 Debug）；`Get` 仍抛，fail-soft 走 `TryGet` + `LogError`；FSM 找不到节点改为打错误。
* **Update**: `/02-程序-前/NuGet与Scriban`、`/02-程序-前/UPM落地` `status` draft → stable，写 `verified.by: human:cjh`。
* **Add**: `/02-程序-前/UPM落地`。Package Manager GitHub URL 装完后统一拷到工程根 `Packages/`；manifest 远程条目留下。
* **Update**: `/02-程序-前/UniTask异步` 包约定改为不删 manifest URL，步骤指向 UPM 落地。
* **Update**: `/02-程序-前/NuGet与Scriban` 拆开两条线：Scriban 只走 NuGet.config / Assets/Packages；`manifest.json` 是 UPM URL 拉包，不是这次的问题。
* **Add**: `/02-程序-前/NuGet与Scriban`。记录 NuGetForUnity + Scriban 7.2.7 接入、UnityTls 出站失败时关梯子并打开系统自动代理。
