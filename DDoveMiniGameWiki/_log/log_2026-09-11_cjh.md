# Directory Update Log

## 2026-09-11
* **Update**: `/02-程序-前/Architecture自动注册`：Architecture 页去掉制作栏，创建 / 生成根类只走 HotBox。
* **Update**: `/02-程序-前/DDoveEditor`、`DDoveUI`、`Architecture自动注册`：总窗新增 HotBox 页编排饼环（环名 + 沿一圈散开）；UI 页不再做投放区。
* **Update**: `/02-程序-前/Architecture自动注册`、`DDoveUI`：架构「生成绑定」改称「生成根类」；饼菜单中心「制作」改「UI」，指令带 UI 前缀。
* **Update**: `/02-程序-前/DDoveEditor`、`DDoveUI`、`Architecture自动注册`：Scene Space Hotbox 改为围着鼠标的饼菜单（中心切制作/架构），挂 Scene 视口 UITK。
* **Update**: `/02-程序-前/DDoveUI` `status` draft → stable，写 `verified.by: human:cjh`；去掉 `需求` tag。
* **Update**: `/02-程序-前/DDoveUI`、`Architecture自动注册`：Space Hotbox 顶栏改为 制作 / 架构，与总窗页签用词对齐。
* **Update**: `/02-程序-前/DDoveEditor` 补「用哪套 UI」：没有特殊原因一律 UIToolkit；`DDoveUI` / `Architecture自动注册` 对齐创建弹窗与 Space 弹出层。
* **Update**: 对照已落地代码核对工程 wiki。`DDoveUI` 正文改「第一刀已落地」（`status` 仍 draft）；`Architecture自动注册` 验收去掉「本刀要做」；`DDoveBoot` 启动场景改读 `DDoveResInitInfo`；`DDoveEditor` / `DDoveFramework-Core` 补 Architecture 落点；Load 例子 `UI/Home` → `WndHome`；按需加载补 `Start` 组；`Architecture自动注册` / `PrimeTween` 去掉已入正式库的 `需求` tag。
* **Update**: `/02-程序-前/Architecture自动注册`：创建弹窗居中；`Name` + 模块文件夹；类名自动补 Model/System/Utility。
* **Update**: `/02-程序-前/DDoveUI`、`Architecture自动注册`：Scene Space Hotbox 顶栏 Tab 切制作 / Architecture。
* **Update**: `/02-程序-前/DDoveEditor`、`Architecture自动注册`、`DDoveUI`：侧栏顺序改 `DDoveEditorNav.Ids`（Architecture / Res / UI）。
* **Update**: `/02-程序-前/Architecture自动注册`、`DDoveUI`：总窗 Architecture 页 / 路径 SO / Hotbox 创建三类已落地；UI 制作滤掉 Architecture 组。
* **Update**: `/02-程序-前/Architecture自动注册` `status` draft → stable，写 `verified.by: human:cjh`。
* **Update**: `/02-程序-前/UPM落地` 例子表补 PrimeTween `1.4.11` 嵌入路径。
* **Update**: `/02-程序-前/Architecture自动注册` 扩总窗 Architecture 页、Editor-only 路径 SO、Hotbox 创建三类。`status` stable → draft；去掉 `verified`。
* **Update**: `/02-程序-前/PrimeTween` `status` draft → stable，写 `verified.by: human:cjh`。
* **Add**: `/02-程序-前/PrimeTween`。接入免费 PrimeTween 1.4.x。只装包；`Game` 业务引用。DDoveUI Base 本刀不引用、不做 ClickScale。`status: draft` + `需求`。
* **Update**: `/02-程序-前/DDoveUI`：PrimeTween 不再写进「不搬 / 不要引用」整库禁令；Base 本刀不引用，业务见 PrimeTween 篇。`status` 仍 draft。
* **Update**: `/02-程序-前/DDoveUI`、`Architecture自动注册`：UI 绑定生成改到 `Generate/UI`。
* **Update**: `/02-程序-前/Architecture与角色`、`DDoveUI`、`DDoveEditor`、`DDoveFramework-Core`：根改为生成 `Generate/Core/GameArchitecture.Generated.cs`；Editor 引用 Core。
* **Update**: `/02-程序-前/Architecture自动注册` 补 `verified.by: human:cjh`。`status` 仍 stable。
* **Update**: `/02-程序-前/Architecture自动注册` `status` draft → stable。未写 `verified`。
* **Add**: `/02-程序-前/Architecture自动注册`。三类特性收集 Model / System / Utility，Editor 生成 `GameArchitecture`；进 Play 前比对清单。不手写 Init。`status: draft` + `需求`。
