# Directory Update Log

## 2026-09-15
* **Update**: `/02-程序-前/DDoveSave` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/DDoveSave`：对齐已落地 Kit / `IGameSave` / Launch；补 `CurrentFileName` 与 SaveData 回落。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/DDoveSave` `status` draft → stable，去掉 `需求` tag，已写 `verified`（human:cjh）。
* **Add**: `/02-程序-前/DDoveSave`。grill 落稿：Kit KV 文件 + `IGameSave`；档名业务传入；Launch 占位 `LoadFile` 后再碰 Architecture；不改 PlayerModel。`status: draft` + `需求`。
* **Update**: `/02-程序-前/UI业务封装` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/UI业务封装`：`ClickSound` 进 `Mono/Sound/`；`GameLaunch` 留根；挂件按种类拆，不按是否 UI、不把粒子 Mask 塞进 `Sound/`。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/UI业务封装` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/UI业务封装`：`Game/Mono` 先平铺，乱了再按种类拆；`GameLaunch` 留根，不开空 `Tween/` / `Launch/`。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/UI业务封装` `status` draft → stable，已写 `verified`（human:cjh）。`GameLaunch` 改到 `Game/Mono`；`Game` 根不再开 `Launch/`。
* **Update**: `/02-程序-前/UI业务封装`：补 `Game/Mono` 与 `ClickSound`（默认点击音走 Kit，不改 `AddClick`）。`status` → draft，摘掉 `verified`。
* **Add**: `/02-程序-前/DDoveAudio`。独立音频根 + Kit；通道 / 设置 / 播放池不进 GameArchitecture。按名走 Res，播放器走 Pool。`status: draft`。
* **Update**: `/02-程序-前/UI业务封装`、`/02-程序-前/CommandQuery` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/UI业务封装`、`/02-程序-前/CommandQuery`：有门槛开窗用一个 `OpenWndCommand<T>` + `TryPass`；`Data` 走实例 `SendCommand`。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/UI业务封装`、`/02-程序-前/CommandQuery`、`/02-程序-前/DDoveUI`、`/02-程序-前/UniTask异步`、`/02-程序-前/DDoveFramework-Core`、`/02-程序-前/Architecture与角色`、`/02-程序-前/IOC容器使用规范` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/UI业务封装`、`/02-程序-前/CommandQuery`、`/02-程序-前/DDoveUI`、`/02-程序-前/UniTask异步`、`/02-程序-前/DDoveFramework-Core`、`/02-程序-前/Architecture与角色`、`/02-程序-前/IOC容器使用规范`：去掉 `UISystem` 与「开窗一律 Command → GetSystem」；业务直调 Kit；Query 改为可复用推导。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/UI业务封装` `status` draft → stable；去掉 `需求` tag；已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/DDovePool` `status` draft → stable，已写 `verified`（human:cjh）。
* **Delete**: `/00-索引/需求稿工作流`、`/02-程序-前/show-me安装`。废弃 Stub 已无现行入链；权威篇在 `/00-索引/Agent/`。
* **Add**: `/02-程序-前/DDovePool`。静态 Kit：C# / GameObject 两套池；默认容量 30；GO 池先 Initialize。不进 Core / IOC。`status: draft`。
* **Update**: `/02-程序-前/UI业务封装`：补开发流程（链 DDoveUI / Atlas / PrimeTween）；门槛加 Tween；验收第 1 条注明 UISystem 落地前不严检。`status` 仍 draft，无 `verified`。
* **Add**: `/02-程序-前/UI业务封装`。grill 落稿：开窗无门槛走 UISystem，有限制走 Command；读先 GetModel / 事件，推导复用再 Query；Launch 第一扇窗仍 Kit。`status: draft` + `需求`。
* **Update**: `/02-程序-前/CommandQuery` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/CommandQuery`：查询统一 Query、业务操作统一 Command（含开窗）；不再写「一次写 / 一次读」当唯一口径。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/DDoveRes`、`/02-程序-前/DDoveRes按需加载`、`/02-程序-前/DDoveRes配置与使用`、`/02-程序-前/DDoveBoot`、`/02-程序-前/Play到WndHome`、`/02-程序-前/TL2场景流式对照`、`/02-程序-前/IOC容器`、`/02-程序-前/TypeEvent`、`/02-程序-前/DDoveFramework-Core`、`/02-程序-前/DDoveUI`、`/02-程序-前/DDoveAtlas`、`/02-程序-前/DDoveCfg`、`/02-程序-前/PrimeTween` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/SpriteAtlasV1与V2`、`/00-索引/Agent/上游skill更新` `status` draft → stable，已写 `verified`（human:cjh）。`/02-程序-前/DDoveEditor`、`/00-索引/Agent/show-me安装`、`/01-策划/index_cjh`、`/03-程序-后/index_cjh` 补 `verified`。
* **Update**: `/02-程序-前/DDoveRes`、`DDoveRes按需加载`、`DDoveRes配置与使用`、`DDoveBoot`、`Play到WndHome`、`TL2场景流式对照`：对齐 Yoo 3.0.5 EditorSimulate / Offline 已接要版本+清单；Host/Web 是远程 Init 未接。抽掉外库路径 / 项目名。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/IOC容器`：去掉「EU Core」；默认具体类型，接口键例外见角色篇；每根 Architecture 一份实例。`/02-程序-前/TypeEvent`、`DDoveFramework-Core` 补多根 / IOC 使用规范。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/DDoveUI`、`DDoveAtlas`、`DDoveCfg`、`PrimeTween`：工程 Playbook 去掉外库项目名；Atlas / 按需加载去掉「实现未齐时以本约定为准」。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/OKF约定` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/OKF约定`：补「流程篇与外库对照」（搬走能否独立执行；对照按主题开，不列外库名单）。`status` stable → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/Architecture与角色`、`/02-程序-前/Architecture自动注册` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/Architecture与角色`、`/02-程序-前/Architecture自动注册`：对齐 [IOC 容器使用规范](/02-程序-前/IOC容器使用规范.md)（玩法根不手写；闭环根手写且不挂 Bind；`Reset` 只清该 T）。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/IOC容器使用规范` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/IOC容器使用规范`：抽掉猫柜 `EUAudio*`；改成「怎么拆独立根」流程。外库对照另开 Reference（样板 TL2）。`status` 仍 draft，无 `verified`。
* **Add**: `/02-程序-前/IOC容器使用规范`。讨论落稿：一条闭环链路配一套 Architecture（一份 IOC）；不要 `new IOCContainer()` 当全局定位器；对照猫柜 Audio。`status: draft`。
