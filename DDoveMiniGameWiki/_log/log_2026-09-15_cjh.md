# Directory Update Log

## 2026-09-15
* **Update**: `/02-程序-前/DDoveRes`、`/02-程序-前/DDoveRes按需加载`、`/02-程序-前/DDoveRes配置与使用`、`/02-程序-前/DDoveBoot`、`/02-程序-前/Play到WndHome`、`/02-程序-前/TL2场景流式对照`、`/02-程序-前/IOC容器`、`/02-程序-前/TypeEvent`、`/02-程序-前/DDoveFramework-Core`、`/02-程序-前/DDoveUI`、`/02-程序-前/DDoveAtlas`、`/02-程序-前/DDoveCfg`、`/02-程序-前/PrimeTween` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/SpriteAtlasV1与V2`、`/00-索引/Agent/上游skill更新` `status` draft → stable，已写 `verified`（human:cjh）。`/02-程序-前/DDoveEditor`、`/00-索引/Agent/show-me安装`、`/01-策划/index_cjh`、`/03-程序-后/index_cjh` 补 `verified`。
* **Update**: `/02-程序-前/DDoveRes`、`DDoveRes按需加载`、`DDoveRes配置与使用`、`DDoveBoot`、`Play到WndHome`、`TL2场景流式对照`：对齐 Yoo 3.0.5 EditorSimulate / Offline 已接要版本+清单；Host/Web 是远程 Init 未接。抽掉外库路径 / 项目名。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/IOC容器`：去掉「EU Core」；默认具体类型，接口键例外见角色篇；每根 Architecture 一份实例。`/02-程序-前/TypeEvent`、`DDoveFramework-Core` 补多根 / IOC 使用规范。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/DDoveUI`、`DDoveAtlas`、`DDoveCfg`、`PrimeTween`：工程 Playbook 去掉外库项目名；Atlas / 按需加载去掉「实现未齐时以本约定为准」。`status` → draft，摘掉 `verified`。
* **Update**: `/00-索引/OKF约定` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/00-索引/OKF约定`：补「流程篇与外库对照」（搬走能否独立执行；对照按主题开，不列外库名单）。`status` stable → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/Architecture与角色`、`/02-程序-前/Architecture自动注册` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/Architecture与角色`、`/02-程序-前/Architecture自动注册`：对齐 [IOC 容器使用规范](/02-程序-前/IOC容器使用规范.md)（玩法根不手写；闭环根手写且不挂 Bind；`Reset` 只清该 T）。`status` → draft，摘掉 `verified`。
* **Update**: `/02-程序-前/IOC容器使用规范` `status` draft → stable，已写 `verified`（human:cjh）。
* **Update**: `/02-程序-前/IOC容器使用规范`：抽掉猫柜 `EUAudio*`；改成「怎么拆独立根」流程。外库对照另开 Reference（样板 TL2）。`status` 仍 draft，无 `verified`。
* **Add**: `/02-程序-前/IOC容器使用规范`。讨论落稿：一条闭环链路配一套 Architecture（一份 IOC）；不要 `new IOCContainer()` 当全局定位器；对照猫柜 Audio。`status: draft`。
