# Directory Update Log

## 2026-09-14
* **Update**: `/02-程序-前/DDoveRes`。`status` draft → stable。`verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveRes`。去掉已删的 `DDoveResPlayExit`；退出只留 `DDoveResLifetime`；边玩边下承受 Abort Warning，不自造停泵。`status` stable → draft。去掉 `verified`。
* **Update**: `/02-程序-前/Play到WndHome`。`status` draft → stable。`verified.by: human:cjh`。
* **Add**: `/02-程序-前/Play到WndHome`。讨论落稿：Play → Boot → Launch → GameLaunch → WndHome；Yoo 3.0.5 Init 后要版本+清单；AfterSceneLoad 只跑第一场。`status: draft`。
* **Update**: `/02-程序-前/DDoveRes`。`status` draft → stable。`verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveRes`。补 `Shutdown`、停 Play 谁调、常驻 `DownloadScheduler` 与 Abort Warning。`status` stable → draft。去掉 `verified`。
* **Add**: `/02-程序-前/SpriteAtlasV1与V2`。讨论落稿：V1/V2 只换 Pack 管线；Mode、Include in Build、Spine 等第三方图集与代数正交。`status: draft`。
* **Update**: `/02-程序-前/DDoveUI`：`status` draft → stable。`verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveUI`：去掉「图集 Kit 还没有」；对照/启动顺序链到 DDoveAtlas。`status` stable → draft。去掉 `verified`。
* **Update**: `/00-索引/OKF约定`、`/00-索引/Agent/需求稿工作流`、`/00-索引/Agent/Agent总览`、`/00-索引/整理人`：本库工作流只记 `draft`/`stable`/`deprecated`；升 `stable` 同步写 `verified`，不再另开「确认」闸门。已有篇改正文走 `/ddove-wiki-update`（回 `draft`）。刷新 `verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveAtlas`：`status` draft → stable；去掉 `需求` tag。`verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveAtlas`：改 Sprite Atlas V2 - Enabled；删一代样例再建；Filter 收 `.spriteatlas` / `.spriteatlasv2`。`status` stable → draft + `需求`。去掉 `verified`。
