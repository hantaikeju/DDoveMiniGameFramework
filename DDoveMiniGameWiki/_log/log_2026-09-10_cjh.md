# Directory Update Log

## 2026-09-10
* **Update**: `/02-程序-前/DDoveUI`：制作页左列条目、右列投放区；配置进 `DDoveHotboxConfig`；Scene 视口 Space 弹出。`status` 仍 draft。
* **Update**: `/02-程序-前/DDoveUI` 程序集表：`Game` 增加 `UnityEngine.UI` / `Unity.TextMeshPro`（生成绑定字段要用）。`status` 仍 draft。
* **Add**: `/02-程序-前/DDoveUI`。第一刀垂直闭环：制作场景 → Prefab → Res 加载 → Launch 开 WndHome。程序集分 Kit / Editor / Game，Boot 不引用 UI。`status: draft` + `需求`。
* **Update**: `/02-程序-前/DDoveRes配置与使用` `status` draft → stable。未写 `verified`。
* **Add**: `/02-程序-前/DDoveRes配置与使用`。总窗 Res 页配默认包 / 加载模式 / 启动场景；Boot 读 SO 进 Launch；运行时 Load。接口仍看 DDoveRes。`status: draft`。
