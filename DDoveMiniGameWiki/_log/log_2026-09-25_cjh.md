# Directory Update Log

## 2026-09-25
* **Update**: `/05-需求/创建LoopList` `status` draft → deprecated。`title` 改为「创建 LoopList（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/创建LoopList`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/05-需求/创建LoopList` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/创建LoopList`。按代码回写入口 `CreateLoopListWindow`。`status` 仍 draft，仍带 `需求`，未写 `verified`。点名 DDoveUI 未列这条 HotBox。
* **Add**: `/05-需求/创建LoopList`。HotBox「创建 LoopList」搭空列表。`status` draft，带 `需求`。
* **Update**: `/05-需求/同名HTML说明页` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/00-索引/Agent/需求稿工作流`、`/00-索引/Agent/Agent总览`。补上 create / change-state 调用 `ddove-show-me`。`status` stable → draft，摘了 `verified`。
* **Update**: `/05-需求/同名HTML说明页`。按落地回写 description。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/同名HTML说明页`。create 与 change-state 各写同名静态 HTML。`status` draft，带 `需求`。
* **Update**: `/05-需求/需求稿留下要什么不要` `status` draft → deprecated。留下要什么、不要和评分。`title` 改为「需求稿留下要什么不要（需求稿）」。用法篇写到 `/00-索引/Agent/需求稿留下要什么不要`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/05-需求/需求稿留下要什么不要` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/需求稿留下要什么不要`。按落地回写 description。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/需求稿留下要什么不要`。做完后需求稿留下要什么、不要、评分和用法篇链接。`status` draft，带 `需求`。
* **Update**: `/05-需求/需求做完生成接口总结` `status` draft → deprecated。得分留在需求稿。`title` 改为「需求做完生成接口总结（需求稿）」。用法篇写到 `/00-索引/Agent/需求做完生成接口总结`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/00-索引/Agent/开工评分` 规格版本 v10 → v11。职责边界放过验收明确要求的升正式。`status` 仍 draft。
* **Update**: `/05-需求/需求做完生成接口总结` `## 评分` 记下开工第 1 轮：v10 打分，总分 80，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/需求做完生成接口总结`。按落地回写 description。点名开工评分过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: 五篇流程升正式（OKF约定、Agent总览、需求稿工作流、开工班组、开篇与清单徽章）。用法篇 `/02-程序-前/WndHomeSample入口` 改成接口总结。本篇收成薄记录，title 改为「WndHome Sample入口（需求稿）」。
* **Add**: `/05-需求/需求做完生成接口总结`。需求做完后的用法篇只写功能与接口；需求稿收成薄记录。`status` draft，带 `需求`。
