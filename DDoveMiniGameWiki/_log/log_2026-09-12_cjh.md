# Directory Update Log

## 2026-09-12
* **Update**: `/00-索引/Agent/改工作区前缀`、`Agent总览`：入口 `ddove-rename-workspace` 已齐；总览表补一行。
* **Update**: `/00-索引/Agent/改工作区前缀`：`status` draft → stable；去掉 `需求` tag；已写 `verified.by: human:cjh`。
* **Add**: `/00-索引/Agent/改工作区前缀`。fork 开新游戏时改四件套前缀；计划 `ddove-rename-workspace`。`status: draft` + `需求`。
* **Update**: `/02-程序-前/DDoveAtlas`：补 `verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveAtlas`：`status` draft → stable；去掉 `需求` tag。未写 `verified`。
* **Add**: `/02-程序-前/DDoveAtlas`。第一刀图集垂直闭环：Excluded 散图、只收 .spriteatlas、独立 Kit late-bind + LoadSpriteAsync。`status: draft` + `需求`。
* **Update**: `/00-索引/Agent/ddove-config`：补 `verified.by: human:cjh`。
* **Update**: `/00-索引/Agent/ddove-config`：`status` draft → stable。未写 `verified`。
* **Add**: `/00-索引/Agent/ddove-config`。配表 Agent 流程。对照 luban/ai 改编。`status: draft`。
* **Update**: `Luban填表`、`DDoveCfg`、`Luban服务端`：对照 csv 源、表头、`ddove-config`、`Defines/builtin.xml`。
* **Update**: `Agent总览`、`上游skill更新`：新增 `ddove-config`，对照 [luban/ai](https://github.com/focus-creative-games/luban/tree/main/ai) 改编。
* **Update**: `/01-策划/Luban填表`、`DDoveCfg`：源改回 csv（Agent 与 Excel 同一份）；去掉 md 转换。
* **Update**: `/01-策划/Luban填表`、`DDoveCfg`：源表改 Markdown，`gen_client` 先转 `_gen` csv 再跑 Luban。
* **Update**: `/01-策划/Luban填表`、`DDoveCfg`：`__beans__` / `__enums__` 也改成 csv（保留嵌套表头）。
* **Update**: `/01-策划/Luban填表`、`DDoveCfg`：业务表示例改为 `demo.item.csv` + `__tables__.csv`，去掉 `#xxx.xlsx`（编辑器打不开）。
* **Update**: `/02-程序-前/DDoveCfg`、`Luban填表`、`Luban服务端`、`DDoveEditor`、`DDoveUI`、`Architecture自动注册`：`DataTables` + `Tools` 并入 `DDoveMiniGameConfig`；去掉总窗 Cfg 页，导表只双击 `gen_client.bat`。
* **Update**: `/02-程序-前/DDoveCfg`、`Luban填表`、`DDoveRes`、`DDoveRes配置与使用`、`DDoveEditor`、`DDoveUI`、`DDoveRes按需加载`、`UPM落地`、`Architecture自动注册`：对照第一刀落地（Tools/Luban v5.1.0、DataTables、DDoveCfg Kit、Launch 先 Load）。
* **Update**: `/02-程序-前/DDoveCfg`、`/01-策划/Luban填表`、`/03-程序-后/Luban服务端`：补 `verified.by: human:cjh`。
* **Update**: `/02-程序-前/DDoveCfg`、`/01-策划/Luban填表`、`/03-程序-后/Luban服务端`：`status` draft → stable；去掉 `需求` tag。未写 `verified`。
* **Add**: `/02-程序-前/DDoveCfg`。第一刀接入 Luban：根目录 `Tools/Luban` + `DataTables`，客户端 Kit 走 Res，Launch 先 Load 再 Architecture。`status: draft` + `需求`。
* **Add**: `/01-策划/Luban填表`。策划只改 `DataTables/Datas`；本刀一张示例表。`status: draft` + `需求`。
* **Add**: `/03-程序-后/Luban服务端`。Stub：conf 已留 server target，不建 server 工程。`status: draft` + `需求`。
* **Add**: `/01-策划/index_cjh`、`/03-程序-后/index_cjh`；职种 `index.md` 挂上清单。
