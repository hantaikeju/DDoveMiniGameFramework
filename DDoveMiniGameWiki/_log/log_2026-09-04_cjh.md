# Directory Update Log

## 2026-09-04
* **Update**: `/02-程序-前/DDoveRes`、`DDoveBoot`、`DDoveEditor`：包名从 Yoo 收集器读；没有则 `DefaultPackage` 直接调 Yoo。Boot 不再拖 Init Info。
* **Update**: `/02-程序-前/DDoveEditor` `status` draft → stable。未写 `verified`。正文改为 UIToolkit 总门面 + `[DDoveEditorPanel]` 挂面板。
* **Update**: `/02-程序-前/DDoveFramework-Core` 落点表：Editor 总门面在 `DDoveFramework/Editor/`。
* **Update**: `/02-程序-前/DDoveRes` 链到总窗 Res 页。
* **Add**: `/02-程序-前/DDoveEditor`。Editor 跟 Extension 散落；菜单 `DDove/Xxx`；根 Editor 等第二处共用再开。`status: draft`。
* **Update**: `/02-程序-前/DDoveFramework-Core` 落点表加 Editor → Extension。
* **Update**: `/02-程序-前/DDoveRes`、`DDoveBoot`：`DDoveResInitInfo` + `InitializeAsync`；Boot 只编排 Extension。
* **Update**: `/02-程序-前/DDoveRes按需加载` `status` draft → stable，写 `verified.by: human:cjh`。
* **Add**: `/02-程序-前/DDoveRes按需加载`。边玩边下、单包裹多 Collector、tag / 配表名字、不接整包 Fsm；对照 H5 预下名单。`status: draft`。
* **Update**: `/02-程序-前/DDoveRes`、`DDoveBoot`、`CoreFsm` 链到按需加载篇。
* **Update**: `/02-程序-前/DDoveBoot` `status` draft → stable。未写 `verified`。
* **Add**: `/02-程序-前/DDoveBoot`。起步场景 `Assets/Scenes/DDoveBoot.unity`（Build Settings index 0，不进 Yoo）；`DDoveBootLogic` 只 `Initialize` / `InitializePackageAsync` / `LoadSceneAsync`。`status: draft`。
* **Update**: `/02-程序-前/DDoveRes` 补 `CreateInitializeOptions`、`LoadSceneAsync`。
