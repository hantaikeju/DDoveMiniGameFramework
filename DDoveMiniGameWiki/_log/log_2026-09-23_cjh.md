# Directory Update Log

## 2026-09-23
* **Update**: `/05-需求/WndHomeSample入口` `## 评分` 记下开工第 1 轮：v10，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/WndHomeSample入口`。按代码回写三行入口与 Launch 顺序（装表、LoadFile、Architecture、NavigateToAsync）。点名过期对照，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/index_cjh`。需求稿目录。
* **Update**: `/02-程序-前/WndHomeSample入口` 改为 Stub，`deprecated`。正文需求稿迁到 `/05-需求/WndHomeSample入口`（仍 `draft` + `需求`）。`01`–`04` 里已是正式的用法篇不搬。
* **Update**: `/00-索引/OKF约定`、`/00-索引/检索指南`、`/00-索引/Agent/需求稿工作流`、`/00-索引/Agent/开工班组`、`/00-索引/Agent/Agent总览`、`/00-索引/Agent/开篇与清单徽章`：需求稿落 `05-需求`；得分记在需求稿；整份做完才生成用法篇。上述篇 `status` → draft，摘了 `verified`。
* **Update**: `/00-索引/Agent/开工评分`：标明只当尺子，得分不另开文件。`status` 仍 draft。
* **Update**: `/02-程序-前/正确路径与程序集方向`、`/00-索引/Agent/开工班组` 已是 `stable`，摘掉残留 `需求` tag；清单徽章去掉 `draft` / `需求`。
* **Update**: `/02-程序-前/index_cjh`、`/00-索引/Agent/index` 把仍为 `draft` 的篇放到「处理中」（WndHome Sample入口、开工评分）。用法查阅留在上面。
* **Update**: `/02-程序-前/LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/LoopList`。正文对齐当前代码：只播入场，去掉扇形 / `ApplyFan` / `ScrollItemTween` 的过期说法。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。只播入场，离开视口不播退场。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。去掉滑动跟手的姿态，进出场改回露出后播一次。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。滑动时进出姿态跟着露出比例，完全进视口才铺平。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。去掉按步距掐进场，边缘继续播进出；只有大半进视口才铺平。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。快滑时竖列表和横列表只要露出约两成就铺平，进场回调也不再把已进画面的格子写回半途。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。快滑时格子过半就铺平，不再停在进场半途。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。`ScrollItemMotion` 移出 `Scroll/Tween`，该目录只留默认样式。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。视口里的格子不再把离场播完留成空行。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。扇形转轴收回到视口边缘，位移只取弧长的 0.22，避免旁边格子转出画面。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。格子改成绕视口中心一个点的扇形，离开时沿同一条弧转出；去掉进场和离场影子。`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。进退场改成绕中心倾斜 28° 转正 / 往另一边倒，不再把影子贴到视口边缘。`status` 仍 draft，未写 `verified`。
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v9 → v10。`status` 仍 draft。
* **Update**: `/02-程序-前/LoopList` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。按代码回写进退场已落地（`PlayEnter` / `PlayExitGhost` / `StopMotion`、`Anim`、`ScrollExitGhost`、竖横样板挂上）。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/02-程序-前/LoopList`。grill 落格子进退场：打开错开滑入并淡入；滑一格影子顺滑动飞出淡出，进来的从对面滑入；一帧跨多格只进场；不再中心缩放；Home 不挂。`status` stable → draft，摘 `verified`，加 `需求`。
