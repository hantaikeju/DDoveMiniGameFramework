# Directory Update Log

## 2026-09-20
* **Update**: `/00-索引/Agent/开工评分`。规格版本 v4 → v5。`status` 仍 draft。
* **Update**: `/02-程序-前/WndHomeSample入口` 摘掉 `需求` tag；`status` 仍 draft，未写 `verified`。
* **Update**: `/02-程序-前/WndHomeSample入口`。按代码回写已落地（WndHome LoopList 入口、HomeEntryRow、WndCollectDemo、Launch Navigate、两页 BtnBack）。`status` 仍 draft，未写 `verified`。
* **Add**: `/02-程序-前/WndHomeSample入口`。grill 落稿：WndHome 改成 LoopList 入口；本刀挂 WndLoopDemo 与 WndCollectDemo；Launch 入栈，点条目 Navigate，每页 Back 回 Home。`status: draft` + `需求`。
* **Update**: `/02-程序-前/DDoveUI` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/DDoveUI`。对照样板改走制作场景：HotBox「创建样板 WndHome」`EnsureScene` 再导出；`WndHome` 只找 Prefab 节点，不运行时拼控件。`status` stable → draft，摘 `verified`。
