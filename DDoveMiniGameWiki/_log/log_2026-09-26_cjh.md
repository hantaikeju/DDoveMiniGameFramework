# Directory Update Log

## 2026-09-26
* **Update**: `/02-程序-前/页内Tab用Toggle` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/页内Tab用Toggle` 改正文。选中外观改链 `TabOnOff`，不再写底图变色。`status` stable → draft，摘了 `verified`。
* **Update**: `/05-需求/页内Tab显OnOff` `status` draft → deprecated。`title` 改为「页内 Tab 显 On Off（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/页内Tab显OnOff`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/02-程序-前/创建LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/创建LoopList`。打开方式改链到创建 LoopList 标题栏，去掉「小窗」。`status` stable → draft，摘了 `verified`。
* **Update**: `/05-需求/页内Tab显OnOff` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/页内Tab显OnOff`。补丁回写 `TabOnOff.Refresh`，窗口不再 `RemoveAllListeners`。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/05-需求/页内Tab显OnOff`。按代码回写 `Game.Mono.TabOnOff`。点名页内 Tab 用 Toggle 过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/05-需求/创建LoopList标题栏` `status` draft → deprecated。`title` 改为「创建 LoopList 标题栏（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/创建LoopList标题栏`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/05-需求/同名HTML说明页` `status` draft → deprecated。`title` 改为「同名HTML说明页（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/00-索引/Agent/同名HTML说明页`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Add**: `/05-需求/页内Tab显OnOff`。每个 Tab 挂 Mono，按 isOn 显示 On 或 Off 两套图和字。`status` draft，带 `需求`。
* **Update**: `/05-需求/创建LoopList标题栏`、`/05-需求/同名HTML说明页` `## 评分` 记下开工第 2 轮：v11，总分 100，过线。对照验收，改动路径无。未另开评分文件。两篇 `status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/页内Tab用Toggle` `status` draft → deprecated。`title` 改为「页内 Tab 用 Toggle（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/页内Tab用Toggle`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/05-需求/页内Tab用Toggle` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/页内Tab用Toggle`。按补丁回写两个 Tab 过渡为 None。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Update**: `/05-需求/页内Tab用Toggle`。按代码回写 Toggle 底色 `0.55` 与 `SetIsOnWithoutNotify`。点名页内 Tab 子页过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/页内Tab用Toggle`。WndTabDemo 两个 Tab 改成同一 ToggleGroup，选中底图白、未选中灰。`status` draft，带 `需求`。
* **Update**: `/05-需求/页内Tab子页` `status` draft → deprecated。`title` 改为「页内 Tab 子页（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/页内Tab子页`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/05-需求/页内Tab子页` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/页内Tab子页`。按代码回写 `OpenChildAsync(Component parent)` 与样板落点。点名 DDoveUI、UI 业务封装、WndHome Sample入口、Play 到 WndHome 过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/页内Tab子页`。子页挂在父页 Content 下用 Tab 切换，关父页先收掉子页。样板从 WndHome 加一行进入。`status` draft，带 `需求`。
* **Update**: `/05-需求/创建LoopList标题栏` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/创建LoopList标题栏`。按代码回写已改成 GetWindow 留一扇。点名创建 LoopList 过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/创建LoopList标题栏`。创建 LoopList 改成编辑器深色标题栏，再次打开聚焦已有那一扇。`status` draft，带 `需求`。
* **Update**: `/05-需求/LoopList点击` `status` draft → deprecated。`title` 改为「LoopList 点击（需求稿）」。留下要什么、不要、评分和用法篇链接。用法篇写到 `/02-程序-前/LoopList点击`，`status` stable，已写 `verified`（human:cjh），`tags` 无 `需求`。
* **Update**: `/02-程序-前/LoopList`、`/02-程序-前/UI业务封装` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/LoopList`、`/02-程序-前/UI业务封装`。点击改为 `LoopList.AddClick`，非空声音名走 `ClickSound.SetSoundName`；`DemoRow` 不再自挂 `onClick`。两篇 `status` stable → draft，摘了 `verified`。`/05-需求/LoopList点击` 去掉「对照未改」句，仍 draft，仍带 `需求`。
* **Update**: `/05-需求/LoopList点击` `## 评分` 记下开工第 1 轮：v11，总分 100，过线。未另开评分文件。`status` 仍 draft，仍带 `需求`。
* **Update**: `/05-需求/LoopList点击`。按代码回写 AddClick 已落地（点击读 Cell.Index，非空声音名走 ClickSound.SetSoundName）。点名 LoopList、UI 业务封装过期，未改正文。`status` 仍 draft，仍带 `需求`，未写 `verified`。
* **Add**: `/05-需求/LoopList点击`。LoopList.AddClick 交出当前下标；传入声音名时挂 ClickSound。`status` draft，带 `需求`。
* **Update**: `/02-程序-前/DDoveEditor` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/DDoveEditor`。编辑器菜单以 `DDove` 为根，允许 `DDove/XX/XX`。`status` stable → draft，摘了 `verified`。
* **Update**: `/02-程序-前/创建LoopList` `status` draft → stable，已写 `verified`（human:cjh）。`tags` 无 `需求`。
* **Update**: `/02-程序-前/创建LoopList`。父节点改为只挂 `UIRoot`，创建后自己拖位置。`status` stable → draft，摘了 `verified`。
