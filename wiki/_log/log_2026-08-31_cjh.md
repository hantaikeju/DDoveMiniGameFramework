# Directory Update Log

## 2026-08-31
* **Update**: 废除 `mgf-wiki` / `mgf-okf-upgrade`。本库 skill 一律 `ddove-`：`ddove-wiki`、`/ddove-wiki-create`、`/ddove-wiki-change-state`、`ddove-okf-upgrade`。历史 `generated.by: process:mgf-okf-init` 与 08-28 log 不改。
* **Update**: `/ddove-wiki-create` 用户只需给生成人短号，目录/标题/正文由 Agent 补齐。
* **Add**: 主动 skill `/ddove-wiki-create`、`/ddove-wiki-change-state`。问答不顺手写 wiki。
* **Add**: 作业闸门：`scripts/check_wiki.py` + Actions 拦缺 `type`/`status`；当天 log 改为 `log_YYYY-MM-DD_<短号>.md`；CODEOWNERS 模板待填 GitHub 名。
* **Update**: 现行约定与前端 Core 篇由 cjh 确认：`verified.by: human:cjh`。`01`/`03`/`04` 不动。
* **Update**: 现行约定与前端 Core 篇 `status` 改为 `stable`。未写 `verified`（仍 unverified）。
* **Update**: 写明 `status` 对照：draft 起草 / stable 正式 / deprecated 废弃。不另加状态位。回答必须带 status。
* **Update**: 根 `index.md` 外链仓库根 `SPEC.md` / `AGENTS.md`（不挪进 wiki）；更正写入指针。补「先 grep、不改他人 generated」。
* **Update**: AI 检索改为直接 grep frontmatter（排除 `_log/`）；`index` 只服务写入。`Core工具` 改名为 `CoreFsm`。
* **Update**: 写 wiki 须先要一次生成人短号；`generated.by: human:<短号>`，禁止 `process:agent` 顶替。
* **Update**: `/02-程序-前/UniTask异步` 按 cjh 清单整理：用法、禁区、Core 边界、嵌入包。
* **Add**: `/02-程序-前/index_cjh`。业务目录篇目改走个人清单；职种 `index.md` 只挂 `index_<短号>`。
* **Update**: 分类 `index.md` 恢复列本目录概念（用来点进篇）；根 index 仍只挂职种目录。
* **Update**: `02-程序-前` 现有概念 `generated.by` 改为 `human:cjh`。
* **Add**: `/00-索引/整理人`。谁整理写 `human:短号`，不用 `index_1` 当篇 ID。
* **Update**: 各层 `index.md` 只挂下一级子目录，不列概念。同一职种多人加篇也不改 index。
* **Add**: `/02-程序-前/UniTask异步`。客户端异步统一 UniTask；Core 不引用。
* **Update**: Wiki 变更史按日拆到 `_log/log_YYYY-MM-DD.md`；根 `log.md` 只作日期索引（SPEC 保留名）。
* **Update**: UniTask 嵌入目录改为 `Packages/com.cysharp.unitask@2.5.11`，去掉 manifest Git URL。
* **Update**: Event / CommandQuery / Fsm 收入 `Core/Mechanism/`。Architecture 仍单独。父目录不叫 Patterns。
* **Add**: `/02-程序-前/CommandQuery`。`ICommand` / `IQuery` 与基类挪到 `Core/CommandQuery/`。
