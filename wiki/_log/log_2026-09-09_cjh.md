# Directory Update Log

## 2026-09-09
* **Update**: `/02-程序-前/DDoveBoot`、`/02-程序-前/DDoveRes按需加载`：资源根改名 `Assets/GameRes/`（Yoo）与 `Assets/GameResExcluded/`（不进收集器）。不用 Unity `Resources/`。
* **Update**: `/02-程序-前/DDoveRes按需加载`：资源根 `Assets/Game/`（Scenes / UI / Atlases / Config）与 `Assets/GameExcluded/`（不进收集器）。
* **Update**: `/02-程序-前/DDoveRes`、`/02-程序-前/DDoveEditor`：Init 读 `DDoveResInitInfo`；Res 页互斥默认包 + 加载模式。
* **Update**: `/02-程序-前/DDoveBoot`、`/02-程序-前/DDoveRes按需加载`：Launch 落点 `Assets/Game/Scenes/Launch.unity`，收集器 `DefaultPackage` / Scene / tag `launch`。
* **Update**: `/02-程序-前/TL2场景流式对照` `status` draft → stable，已写 `verified`（human:cjh）。
* **Add**: `/02-程序-前/TL2场景流式对照`。提取 TL2 分块流式、sceneabdata、进场景暂停延迟加载，供以后大世界参考。本库小游戏默认仍边玩边下。`status: draft`。
* **Update**: `/02-程序-前/DDoveRes按需加载` 链到场景流式对照。
* **Add**: `/00-索引/Agent/上游skill更新`。对照 mattpocock/skills Release 改编 `ddove-*`；不订阅读包、不 `npx skills update`。`status: draft`。
* **Update**: `/00-索引/Agent/Agent总览`、`/00-索引/Agent/需求稿工作流`、`/00-索引/Agent/工作流迁移` `status` draft → stable，已写 `verified`（human:cjh）。
* **Add**: `ddove-work`。问答 / 排障 / 改代码；`ddove-wiki` 改为只检索。
* **Add**: `/ddove-help`。整套工作流路由器，只指路不代跑。`AGENTS.md` / `Agent总览` / `OKF约定` 已挂。
* **Add**: `/00-索引/Agent/工作流迁移`。换引擎（Godot 等）只拷 AI 层 + 空 `01`–`04`，不拷 Unity 业务篇。`status: draft`。
* **Add**: `/00-索引/Agent/Agent总览`、`/00-索引/Agent/需求稿工作流`、`/00-索引/Agent/show-me安装`。工程 `01`–`04` 与 AI 用法 `00-索引/Agent/` 分轨。`status: draft`（总览、需求稿）。
* **Update**: `show-me安装`、`需求稿工作流` 旧路径改为 Stub。`OKF约定` 增「两层 wiki」。`index_cjh` 去掉 show-me。
* **Add**: `/00-索引/需求稿工作流`（后迁至 Agent，旧路径留 Stub）。
* **Update**: `ddove-wiki` 瘦身；新增 `ddove-grill`、`ddove-writing-for-agents`。`AGENTS.md` 补 6–7 条。
