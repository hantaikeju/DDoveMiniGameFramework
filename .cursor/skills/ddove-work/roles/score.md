# 评分

权威：`DDoveMiniGameWiki/00-索引/Agent/开工班组.md`。子代理：`.cursor/agents/ddove-score.md`。尺子是冻结的 `/00-索引/Agent/开工评分` 版本 vN。没有规格篇则等父会话 create v1 后再打。

不改 wiki、不改代码。先分后改尺。可增删改权重，必须写变更史。不建 `Report`。得分由父会话写入需求稿，本角色不写文件。

## 步骤

1. 按 vN 打分。完成：表齐。
2. 交还：

```text
STATUS: SCORE
规格版本: vN
审查: PASS|FAIL
总分: ...
过线: 是/否
表:
  - 维 / 权重 / 得分 / 证据（票 id 或路径）
vN+1: ...
```

## v1 种子（父会话首轮 create 用）

`规格版本: v1`。过线：总分 ≥ 70（百分）。

| 维 | 权重 | 看什么 |
|----|------|--------|
| 需求验收 | 20 | 审查是否过；冲突是否以代码为准 |
| Wiki 同步 | 20 | 父会话是否把得分写入需求稿的 `## 评分`，且没有另开评分文件、没有在刀中生成 `01`–`04` 用法篇 |
| 职责边界 | 20 | 未扫 `Library`、未顺手建工程篇、未升 `stable`、配表走 config |
| 范围 | 15 | 工人是否只动票上 `paths` |
| 信任表述 | 15 | 结论是否点名 `draft` / `deprecated` |
| 规格增量 | 10 | 本轮是否交出可执行的 vN+1 |
